# Architecture & Mixins (MC 26.2)

🌐 **Languages**: [[🇺🇸 English|Home]] | [[🇨🇳 简体中文|zh_cn-Home]] | [[🇭🇰 繁體中文|zh_tw-Home]] | [[🇷🇺 Русский|ru_ru-Home]] | [[🇪🇸 Español|es_es-Home]] | [[🇩🇪 Deutsch|de_de-Home]] | [[🇫🇷 Français|fr_fr-Home]] | [[🇧🇷 Português|pt_br-Home]] | [[🇯🇵 日本語|ja_jp-Home]] | [[🇮🇩 Bahasa Indonesia|id_id-Home]] | [[🇰🇷 한국어|ko_kr-Home]]

> 📌 **Repository Source Disclaimer**: The documentation in this Wiki reflects the **current source code state in the repository**, which may include recent unreleased commits or developmental features ahead of public release builds on CurseForge and Modrinth.

## 1. Official Technical Infobox
| Parameter | Specification |
| :--- | :--- |
| **Root Package** | `com.dasik.mcagenderbridge` |
| **Mixin Config** | `mca_female_gender_bridge.mixins.json` |
| **Bytecode Compatibility** | `JAVA_25` |
| **Client Mixins** | 3 Classes (`PlayerEntityExtendedModelMixin`, `VillagerEntityBaseModelMCAMixin`, `VillagerLikeEntityMCARendererMixin`) |
| **Side Safety** | Strict `@Environment(EnvType.CLIENT)` separation |
| **Runtime Guard** | `ModVersionGuard.java` (ClassLoader Integrity Gate) |

---

## 2. Step-by-Step Mixin Execution & Bytecode Workflow

```
com.dasik.mcagenderbridge/
├── McaFemaleGenderBridge.java           [ModInitializer - Core entrypoint & guard check]
├── ModVersionGuard.java                 [Runtime Knot/Context ClassLoader validator]
│
├── client/
│   └── McaFemaleGenderBridgeClient.java [ClientModInitializer - Client subsystem startup]
│   └── render/
│       └── McaBreastRenderLayer.java    [Mojang RenderLayer attached to MCA renderer]
│
├── genetics/
│   └── GeneticsBridge.java              [Allele crossover & mutation calculations]
│
├── physics/
│   └── McaBreastPhysicsEngine.java      [Concurrent spring-mass-damper physics engine]
│
├── render/
│   └── McaBreastModelBox.java           [Custom 3D model box geometry & LayerDefinition]
│
└── mixin/
    ├── PlayerEntityExtendedModelMixin.java  [Suppresses MCA player static breast mesh]
    ├── VillagerEntityBaseModelMCAMixin.java [Suppresses MCA villager static breast mesh]
    └── VillagerLikeEntityMCARendererMixin.java [Attaches McaBreastRenderLayer to MCA renderer]
```

---

## 3. Bytecode Mixin Target Matrix

### 1. `PlayerEntityExtendedModelMixin`
- **Target**: `net.conczin.mca.client.model.PlayerEntityExtendedModel` (`remap = false`)
- **Shadow Fields**: `public ModelPart breasts`, `public ModelPart breastsWear`
- **Injections**:
  1. `setupAnim(AvatarRenderState state, CallbackInfo ci)` at `@At("RETURN")`
  2. `copyVisibility(HumanoidModel<?> model, CallbackInfo ci)` at `@At("RETURN")`
  3. `setAllVisible(boolean visible, CallbackInfo ci)` at `@At("RETURN")`
- **Operation**: Sets `this.breasts.visible = false` and `this.breastsWear.visible = false` across all animation and visibility update calls.

### 2. `VillagerEntityBaseModelMCAMixin`
- **Target**: `net.conczin.mca.client.model.VillagerEntityBaseModelMCA` (`remap = false`)
- **Shadow Field**: `public final ModelPart breasts`
- **Injections**:
  1. `setupAnim(VillagerRenderState state, CallbackInfo ci)` at `@At("RETURN")`
- **Operation**: Sets `this.breasts.visible = false` to suppress MCA's default un-animated breast cubes.

### 3. `VillagerLikeEntityMCARendererMixin`
- **Target**: `net.conczin.mca.client.render.VillagerLikeEntityMCARenderer` (`remap = false`)
- **Superclass**: `HumanoidMobRenderer<Mob, VillagerRenderState, VillagerEntityModelMCA>`
- **Injections**:
  1. `<init>(EntityRendererProvider.Context ctx, VillagerEntityModelMCA model, CallbackInfo ci)` at `@At("RETURN")`
- **Operation**: Calls `this.addLayer(new McaBreastRenderLayer(this))` to register the dynamic rendering layer.

---

## 4. Visual Mixin Interception Flowchart

```
   [ MCA Villager Render Initialization ]
                     |
                     v
   [ VillagerLikeEntityMCARenderer.<init> ]
                     |
                     +---> [ VillagerLikeEntityMCARendererMixin ]
                                   |
                                   v (Injects at RETURN)
                           [ addLayer(McaBreastRenderLayer) ]
                                   |
   [ Villager Model Animation Frame ]
                     |
                     v
   [ VillagerEntityBaseModelMCA.setupAnim ]
                     |
                     +---> [ VillagerEntityBaseModelMCAMixin ]
                                   |
                                   v (Injects at RETURN)
                           [ breasts.visible = false ] (Suppresses static box)
                                   |
   [ Scene Render Phase ]
                     |
                     v
   [ McaBreastRenderLayer.submit() ] (Renders Dynamic Scaled Mesh with Physics)
```

---

## 5. Mixin Configuration Schema (`mca_female_gender_bridge.mixins.json`)

```json
{
  "required": true,
  "package": "com.dasik.mcagenderbridge.mixin",
  "compatibilityLevel": "JAVA_25",
  "mixins": [],
  "client": [
    "PlayerEntityExtendedModelMixin",
    "VillagerEntityBaseModelMCAMixin",
    "VillagerLikeEntityMCARendererMixin"
  ],
  "injectors": {
    "defaultRequire": 1
  }
}
```

---

## 6. Exhaustive Method Signature Reference

| Mixin Class | Target Method | Injection Point | Injected Method Signature |
| :--- | :--- | :--- | :--- |
| `PlayerEntityExtendedModelMixin` | `setupAnim` | `@At("RETURN")` | `void mcagenderbridge$suppressMcaPlayerBreastsSetupAnim(AvatarRenderState, CallbackInfo)` |
| `PlayerEntityExtendedModelMixin` | `copyVisibility` | `@At("RETURN")` | `void mcagenderbridge$suppressMcaPlayerBreastsCopyVisibility(HumanoidModel<?>, CallbackInfo)` |
| `PlayerEntityExtendedModelMixin` | `setAllVisible` | `@At("RETURN")` | `void mcagenderbridge$suppressMcaPlayerBreastsSetAllVisible(boolean, CallbackInfo)` |
| `VillagerEntityBaseModelMCAMixin` | `setupAnim` | `@At("RETURN")` | `void mcagenderbridge$suppressMcaVillagerBreasts(VillagerRenderState, CallbackInfo)` |
| `VillagerLikeEntityMCARendererMixin` | `<init>` | `@At("RETURN")` | `void mcagenderbridge$attachBreastRenderLayer(EntityRendererProvider.Context, VillagerEntityModelMCA, CallbackInfo)` |

---

## 7. Developer & Side-Safety Standards

1. **Strict Client Segregation**: All rendering classes (`McaBreastRenderLayer`, `McaBreastModelBox`, `McaFemaleGenderBridgeClient`) and mixins are annotated with `@Environment(EnvType.CLIENT)` or registered exclusively under client mixin sets.
2. **Knot ClassLoader Compatibility**: `ModVersionGuard` inspects the thread context classloader to prevent premature class initialization or linkage errors during Fabric server startup.
